import 'package:flutter/material.dart';

//الصوتيات
class AlsawtiaatScreen extends StatelessWidget {
  const AlsawtiaatScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: const Center(
        child: Text("data"),
      ),
    );
  }
}
