import 'package:flutter/material.dart';

//الادعيه
class AladaeihScreen extends StatelessWidget {
  const AladaeihScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: const Center(
        child: Text("data"),
      ),
    );
  }
}
